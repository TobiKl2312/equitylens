# Copy to .env and fill in. Never commit .env.

# Postgres (matches docker-compose defaults)
DATABASE_URL=postgresql+asyncpg://equitylens:equitylens@localhost:5432/equitylens

# SEC EDGAR requires a descriptive User-Agent with a contact address
EDGAR_USER_AGENT="EquityLens/0.1 (your-email@example.com)"

# Week 2+ (RAG)
ANTHROPIC_API_KEY=
VOYAGE_API_KEY=
